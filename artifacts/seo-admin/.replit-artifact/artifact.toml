kind = "web"
previewPath = "/"
title = "SEO Admin Panel"
version = "1.0.0"
id = "artifacts/seo-admin"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26154

[services.development]
run = "pnpm --filter @workspace/seo-admin run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/seo-admin", "run", "build" ]
publicDir = "artifacts/seo-admin/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26154"
BASE_PATH = "/"
